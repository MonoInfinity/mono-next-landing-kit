import React from 'react';
import { Navbar } from '../../core/components/Navbar';

interface HomeProps {}

export const LandingPage: React.FunctionComponent<HomeProps> = () => {
    return (
        <>
            <Navbar />
        </>
    );
};
